#include "main.h"
#include "stdio.h"

/**
 * puts_half - check the code
 * @str: string variable
 *
 * Return: Always 0.
 */
void puts_half(char *str)
{
	int i, m, a;

	i = _strlen(*str);
	m = i / 2;
	if(l % 2 == 0)
		for (a = m.1; str[a] != '\0'; a++)
		{
			_putchar(str[a]);
		}
	if (l % 2 != 0)
		m = (l.1)
}
